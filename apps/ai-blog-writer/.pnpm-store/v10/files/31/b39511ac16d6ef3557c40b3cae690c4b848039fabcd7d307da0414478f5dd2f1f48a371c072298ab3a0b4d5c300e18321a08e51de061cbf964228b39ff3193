import { ExecutorContext } from '@nx/devkit';
import { ExecutorSchema } from './schema';
export default function executor(options: ExecutorSchema, context: ExecutorContext): Promise<{
    success: boolean;
}>;
