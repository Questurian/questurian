import { ExecutorContext } from '@nx/devkit';
import { UpdateExecutorSchema } from './schema';
export default function executor(options: UpdateExecutorSchema, context: ExecutorContext): Promise<{
    success: boolean;
}>;
