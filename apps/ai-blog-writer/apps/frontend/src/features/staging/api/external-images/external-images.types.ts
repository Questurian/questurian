// Re-export shim. External-images moved to shared/images/external (ADR 0020).
// Kept so existing staging/locationDocuments imports keep working; removed when
// the staging migration lands.
export * from '../../../../shared/images/external/external-images.types'
