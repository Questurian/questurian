{
  "name": "@questurian/dashboard",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "dev:clean": "pnpm install --filter @questurian/dashboard... && bun run --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "lint": "pnpm exec tsc --noEmit -p tsconfig.json",
    "test": "echo 'No tests configured yet for dashboard (explicitly skipped)'"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "ink": "^6.6.0",
    "ink-big-text": "^2.0.0",
    "ink-gradient": "^3.0.0",
    "ink-spinner": "^5.0.0",
    "react": "^19.2.3",
    "react-devtools-core": "^6.1.5"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/react": "^19.2.9",
    "typescript": "^5.9.3"
  }
}
