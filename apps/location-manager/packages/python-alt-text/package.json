{
  "name": "@questurian/lm-python-alt-text",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "node scripts/show-commands.cjs && pnpm run py:ensure && .venv/bin/python -m uvicorn app:app --host 0.0.0.0 --port 8642 --reload",
    "dev:clean": "lsof -ti:8642 | xargs kill -9 2>/dev/null || true; UV_CACHE_DIR=../../../../.uv-cache uv venv --python \"$(node scripts/resolve-python.cjs)\" --clear .venv && UV_CACHE_DIR=../../../../.uv-cache uv pip install --python .venv/bin/python -r requirements.txt && node scripts/show-commands.cjs && .venv/bin/python -m uvicorn app:app --host 0.0.0.0 --port 8642 --reload",
    "build": "echo 'No build step for Python'",
    "lint": "python3 -m py_compile app.py",
    "test": "python3 -c \"print('Python 3 available'); import sys; print(f'Version: {sys.version}')\"",
    "py:dev": ".venv/bin/python app.py",
    "py:ensure": "UV_CACHE_DIR=../../../../.uv-cache uv venv --python \"$(node scripts/resolve-python.cjs)\" --allow-existing .venv && (.venv/bin/python -c \"import uvicorn\" >/dev/null 2>&1 || UV_CACHE_DIR=../../../../.uv-cache uv pip install --python .venv/bin/python -r requirements.txt)",
    "py:install": "UV_CACHE_DIR=../../../../.uv-cache uv venv --python \"$(node scripts/resolve-python.cjs)\" --clear .venv && UV_CACHE_DIR=../../../../.uv-cache uv pip install --python .venv/bin/python -r requirements.txt",
    "py:install:system": "pip3 install -r requirements.txt",
    "test:connection": "bun run test-connection.ts"
  }
}
