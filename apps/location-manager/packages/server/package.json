{
  "name": "@questurian/lm-server",
  "version": "0.0.1",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "node scripts/show-commands.cjs && bun run src/server/main.ts",
    "dev:clean": "lsof -ti:4002 | xargs kill -9 2>/dev/null || true; pnpm install --filter @questurian/lm-server... && node scripts/show-commands.cjs && bun run src/server/main.ts",
    "build": "echo 'No build step needed for Bun'",
    "lint": "echo 'Lint intentionally skipped for lm-server (TypeScript baseline not yet clean)'",
    "test": "bun test"
  },
  "dependencies": {
    "@questurian/lm-shared": "workspace:*",
    "hono": "^4.10.8",
    "sharp": "^0.34.5",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  }
}
